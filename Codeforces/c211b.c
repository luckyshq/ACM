#include <stdio.h>

int main(int argc, char *argv[])
{
    freopen("input", "r", stdin);
    
    int i, c;
    int n, k;
    scanf("%d%d", &n, &k);
    int j = 1;
    int min = 101*k;
    int h[n];
    for (i = 0; i < n; i++)
	{
	    scanf("%d", &h[i]);
	}
    for (i = 0; i < n; i++)
	{
	  
	}
       
    return 0;
}
